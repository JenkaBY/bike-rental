<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.5.xsd">

    <changeSet id="event_publication_archive.create-table" author="bikerental">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="event_publication_archive"/>
            </not>
        </preConditions>

        <createTable tableName="event_publication_archive">
            <column name="id" type="uuid">
                <constraints nullable="false" primaryKey="true"/>
            </column>
            <column name="listener_id" type="text">
                <constraints nullable="false"/>
            </column>
            <column name="event_type" type="text">
                <constraints nullable="false"/>
            </column>
            <column name="serialized_event" type="text">
                <constraints nullable="false"/>
            </column>
            <column name="publication_date" type="TIMESTAMP WITH TIME ZONE">
                <constraints nullable="false"/>
            </column>
            <column name="completion_date" type="TIMESTAMP WITH TIME ZONE"/>
            <column name="status" type="text"/>
            <column name="completion_attempts" type="int"/>
            <column name="last_resubmission_date" type="TIMESTAMP WITH TIME ZONE"/>
        </createTable>

        <!-- Create index on completion_date using standard index -->
        <createIndex indexName="event_publication_archive_by_completion_date_idx" tableName="event_publication_archive">
            <column name="completion_date"/>
        </createIndex>

        <!-- PostgreSQL-specific hash index for serialized_event -->
        <sql>
            CREATE INDEX IF NOT EXISTS event_publication_archive_serialized_event_hash_idx
                ON event_publication_archive USING hash (serialized_event);
        </sql>
    </changeSet>

</databaseChangeLog>
